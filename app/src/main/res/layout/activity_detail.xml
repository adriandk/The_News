<?xml version="1.0" encoding="utf-8"?>
<androidx.swiperefreshlayout.widget.SwipeRefreshLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/swipe_refresh"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.DetailActivity">

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <RelativeLayout
            android:background="@color/colorBlack"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="16dp">

            <com.google.android.material.button.MaterialButton
                android:id="@+id/back_button"
                android:layout_width="wrap_content"
                android:layout_height="38dp"
                android:background="@drawable/custom_button"
                android:text="@string/back"
                android:textColor="@android:color/black"
                app:backgroundTint="@android:color/white"
                app:icon="@drawable/back_icon"
                app:iconTint="@color/colorBlack" />

            <TextView
                android:id="@+id/title_detail"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/back_button"
                android:layout_marginTop="22dp"
                android:fontFamily="sans-serif"
                android:text="Fitbit adds ECG and stress-level scanning to its Charge fitness tracker"
                android:textColor="@android:color/white"
                android:textSize="28sp"
                android:textStyle="bold" />

            <TextView
                android:id="@+id/date_detail"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/title_detail"
                android:layout_marginTop="8dp"
                android:text="1 Januari 2020"
                android:textColor="@color/grey"
                android:textSize="18dp" />

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/image_detail"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/date_detail"
                android:layout_marginTop="16dp"
                android:adjustViewBounds="true"
                android:contentDescription="@string/news_image"
                android:scaleType="fitXY"
                android:src="@drawable/contoh"
                app:shapeAppearanceOverlay="@style/ShapeAppearanceOverlay.App.CornerSize" />

            <TextView
                android:id="@+id/description_detail"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/image_detail"
                android:layout_marginTop="16dp"
                android:text="Fitness band market share is undoubtedly contracting, thanks in no small part to the massive popularity of smartwatches. But 13.1 million overall shipments in Q1 2021 is nothing to sneeze at. People are still buying non-watch fitness trackers, due to their lower price and non-invasiveness. Announced this morning via the Google Keyword blog, the latest version of Fitbit’s Charge line looks to further blur the line line between the categories. The latest version of the premium fitness band adds a color touchscreen, along with ECG (heart) and EDA (stress) sensors."
                android:textColor="@android:color/white"
                android:textSize="18dp" />

        </RelativeLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.swiperefreshlayout.widget.SwipeRefreshLayout>